# AWS DevOps Project Environment Variables
# Copy this file to .env and customize the values
# Usage: source .env && ./deploy.sh dev deploy

# AWS EC2 Key Pair Name (create first with: aws ec2 create-key-pair)
export TF_VAR_key_pair_name="myapp-dev-keypair"

# Database Password (use a strong, secure password)
export TF_VAR_db_password="your-secure-password-123"

# Optional: Database Username (defaults to 'postgres' if not set)
# export TF_VAR_db_username="postgres"

# Note: Ensure your AWS credentials are configured via 'aws configure'
# or AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY environment variables
